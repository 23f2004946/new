name: Test Workflow

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 23f2004946@ds.study.iitm.ac.in
        run: echo "Hello, world!"
